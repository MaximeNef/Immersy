import Head from 'next/head'
import { useState } from 'react'
import ContactDirect from '../../components/contact/contactDirect'
import ContactezNous from '../../components/contact/contactezNous'
import ModalForm from '../../components/contact/modalForm'
import Navbar from '../../components/navbar'

const Contact = () => {
  const [hideModal, setHideModal] = useState(false)

  return (
    <Navbar current={'contact'}>
      <Head>
        <title>Immersy - Contact</title>
        <meta name="description" content="Generated by create next app" />
        <link rel="icon" href="/favicon.ico" />
      </Head>
      <div className="md:mx-80">
        {hideModal ? (
          <div className="mx-5 mt-32 md:mt-28">
            <ModalForm setHideModal={setHideModal} hideModal={hideModal} />
          </div>
        ) : (
          <>
            <ContactezNous hideModal={hideModal} setHideModal={setHideModal} />
            <ContactDirect />
          </>
        )}
      </div>
    </Navbar>
  )
}

export default Contact
